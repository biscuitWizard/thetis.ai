---
name = "Sub-agents and delegation"
brief = "How an agent spawns sub-agents, waits on them, and how a child's work is isolated, routed, settled and shown in the UI."
when_to_use = "Use when working on sub-agent delegation in Thetis: spawning, waiting on, or inspecting a sub-agent, child-session routing, or the nested sub-agent block in the transcript. Use it also to explain why a child cannot delegate further or why a wait always has a deadline. Not for the ordinary turn loop (turn-lifecycle) or the general procedure for editing yourself (careful-surgery)."
tags = ["subagent", "subagents", "delegation", "spawn", "spawn_agent", "wait", "agent_status", "agent_transcript", "cancel_agent", "delegation WIT interface", "subagents registry", "agent profiles", "[subagents]", "fan-out", "parallel", "concurrency", "child session", "orchestrator-worker"]
version = 2
---
