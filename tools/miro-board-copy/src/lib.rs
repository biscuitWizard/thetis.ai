wit_bindgen::generate!({world:"tool",path:"../../wit",generate_all}); mod miro; use miro::Miro; use serde_json::{json,Value}; struct Component; impl Guest for Component{fn describe()->ToolManifest{ToolManifest{name:"miro-board-copy".into(),description:"Copy a Miro board using PUT /boards?copy_from=.".into(),args_schema_json:json!({"type":"object","properties":{"copy_from":{"type":"string"},"body":{"type":"object"}},"required":["copy_from"],"additionalProperties":false}).to_string(),capabilities:vec!["http".into(),"group:miro".into()]}} fn invoke(_:String,a:String,c:String)->Result<String,String>{let a=miro::args(&a)?;let b=a.get("body").filter(|v|v.is_object()).cloned().unwrap_or_else(||json!({}));let q=vec![("copy_from".into(),miro::req(&a,"copy_from")?.into())];Ok(miro::normalized(&Miro::from_config(&c)?.request("PUT","/boards",&q,Some(&b),false)?))}} export!(Component);
