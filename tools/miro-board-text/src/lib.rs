wit_bindgen::generate!({world:"tool",path:"../../wit",generate_all});mod miro;use miro::Miro;use serde_json::json;struct Component;impl Guest for Component{fn describe()->ToolManifest{ToolManifest{name:"miro-board-text".into(),description:"Extract bounded text fields from Miro board items.".into(),args_schema_json:json!({"type":"object","properties":{"board_id":{"type":"string"},"cursor":{"type":"string"},"limit":{"type":"integer"}},"required":["board_id"],"additionalProperties":false}).to_string(),capabilities:vec!["http".into(),"read-only".into(),"group:miro".into()]}}fn invoke(_:String,a:String,c:String)->Result<String,String>{let a=miro::args(&a)?;let p=format!("/boards/{}/items",miro::enc(miro::req(&a,"board_id")?));let(rows,next)=Miro::from_config(&c)?.paginate(&p,miro::q(&a,&["cursor"]),miro::limit(&a),"cursor")?;let mut text=vec![];for x in &rows{miro::collect_text(x,&mut text)}Ok(miro::normalized(&json!({"text":text,"items_scanned":rows.len(),"next":next})))}}export!(Component);
